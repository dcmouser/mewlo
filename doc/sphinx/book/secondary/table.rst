Table and Paging Helper
=======================


There are many times when we want to display some tabular data which the user can filter/sort/search/page.
We would like to centralize an API library for doing this kind of stuff consistently and efficiently.
This system would also be used for non-tabular paged displays.
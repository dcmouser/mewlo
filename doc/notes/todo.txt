
//---------------------------------------------------------------------------
3/3/14
we had a confusing error when running initial tests after installation on a new install, because some testsite directories did not exist that were trying to be written to.
for example, the testsite1/database directory.  fix code so that the directories are create or clearly errored at startup.
//---------------------------------------------------------------------------


//---------------------------------------------------------------------------
3/4/14
setup/install code
more involved check of good system state at startup
//---------------------------------------------------------------------------



//---------------------------------------------------------------------------
3/6/14
See escapequote_stringval_for_sql() in mdbmanager_sqlalchemy.py and decide how ot handle raw vs sqlalchemy high level combined expressions
//---------------------------------------------------------------------------



//---------------------------------------------------------------------------
3/9/14
On group list page, each group should link to its info page.
//---------------------------------------------------------------------------


//---------------------------------------------------------------------------
3/9/14
Refactor and simplify code
write syncdb-like functions
//---------------------------------------------------------------------------


//---------------------------------------------------------------------------
3/9/14
install virtualenv for django
make setup/startup as clean as django
//---------------------------------------------------------------------------


//---------------------------------------------------------------------------
3/12/14
Our new asset file replacement mirror directory thing works fine for any files installed under main mewlo/ directory.
But it does not solve the issue of addon packages (mpacks) install through eggs that may be OUTSIDE of the main mewlo directory.
I think we could solve this by having a site create additional mirror directories for each such package using an alias to refer to that mpack's home dir.
But it might be nice to offer a simpler non-config approach to this, by letting user specify a single MIRROR directory, and having mewlo auto map package found to a subdir.
So core/ would be one subdir, and then each other addon would get its own, whether its installed as an egg outside of mewlo main dir or not.
//---------------------------------------------------------------------------





//---------------------------------------------------------------------------
3/13/14
Write nice LINK (not just url) generator helper functions callable from templates
//---------------------------------------------------------------------------




//---------------------------------------------------------------------------
3/14/14
We need some more consistent logic for what goes in config settings and what is created as objects directly
//---------------------------------------------------------------------------




//---------------------------------------------------------------------------
3/15/14
We nicely track startup STAGE, but we removed the general stage tracking that is useful for debugging; put generic stage tracking back in 
//---------------------------------------------------------------------------


//---------------------------------------------------------------------------
3/15/14
Add a kind of url namespace stuff to routes.
//---------------------------------------------------------------------------



//---------------------------------------------------------------------------

The externaltools directory (package) holds some helper code and tools that might be used by scripts and services independently of the mewlo system.

That is, this is helper stuff that might be used for building addons, etc.  This code is NOT called by a normal mewlo system itself.